using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Classe pour la navigation en 2D
/// </summary>
public class NavigationCharacterControler : MonoBehaviour
{
    /// <summary>
    /// Composant CharacterController pour gérer les déplacements
    /// </summary>
    public CharacterController Character;

    /// <summary>
    /// Vitesse de déplacement
    /// </summary>
    public float Speed = 0;

    // Update is called once per frame
    void Update()
    {
        bool walk = false;
        // Si appui sur flèche directionnelle, déplacement dans la direction correspondante
        if (...)
        {
            Character. ...
            ...
        }
        if (...)
        {
            ...
        }
        if (...)
        {
            ...
        }
        if (...)
        {
            ...
        }
    }
}
